# Optional host development/debug ports. Use with -f compose.yaml -f compose.host.yaml.
services:
  db:
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-5432}:5432"
  backend:
    ports:
      - "127.0.0.1:${BACKEND_PORT:-8080}:8080"
